{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "chrome",
            "request": "launch",
            "name": "Launch Chrome against localhost",
            "url": "http://local.bpanchenko.ru:81",
            "webRoot": "${workspaceFolder}"
        }
    ]
}