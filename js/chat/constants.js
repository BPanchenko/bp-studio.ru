const API_PATH = `${window.location.origin}/api`

const URL_CLIENT = `${API_PATH}/client`
const URL_MESSAGES = `${API_PATH}/messages`
const URL_SESSION = `${API_PATH}/session`

export {
    URL_CLIENT,
    URL_MESSAGES,
    URL_SESSION
}